Drop `og-band.jpg` here (referenced by the `og:image` meta tag in `../index.html`)
once you have a band photo you want showing up in link previews. Not required
for the site to work — just for social/iMessage link unfurls.
